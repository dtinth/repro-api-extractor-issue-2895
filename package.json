{
  "name": "repro",
  "scripts": {
    "test": "api-extractor run --diagnostics && node validate.js"
  },
  "devDependencies": {
    "@microsoft/api-extractor": "^7.18.7",
    "@types/invariant": "^2.2.35",
    "invariant": "^2.2.4"
  }
}
